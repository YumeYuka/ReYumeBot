product: windows/app

settings:
  kotlin:
    serialization: json
  ktor: enabled
  native:
    entryPoint: main

dependencies:
  - $ktor.client.core
  - io.ktor:ktor-client-curl:3.4.3
  - $ktor.client.contentNegotiation
  - $ktor.serialization.kotlinx.json
  - org.jetbrains.kotlinx:kotlinx-io-core:0.9.1